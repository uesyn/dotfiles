apiVersion: v1
kind: Service
metadata:
  annotations:
    devk.uesyn.dev/ssh-user: "devbox"
  name: devbox
spec:
  type: NodePort
  selector:
    app: devbox
  ports:
  - name: ssh
    port: 22
    targetPort: ssh
