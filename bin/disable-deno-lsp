#!/usr/bin/env bash

dir="$(pwd)"
while [[ -n "${dir}" ]]; do
  if [[ -d ${dir}/.git ]]; then
    mkdir -p ${dir}/.vim-lsp-settings
    echo '{"deno": {"disabled": true}}' > "${dir}/.vim-lsp-settings/settings.json"
  fi
  dir="${dir%/*}"
done
